import ReactDOM from "react-dom/client";
import PopupViewer from '../components/PopupViewer'

const root = document.createElement("div");
document.body.appendChild(root);

ReactDOM.createRoot(root).render(<PopupViewer />);